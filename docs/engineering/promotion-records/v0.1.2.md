# Promotion record — v0.1.2

- Version / git tag: 0.1.2 / `v0.1.2`
- Artifact SHA-256: `660206b722e3f284ea447758fc3434af74744c6a2fbb31ff1e203e6a8c5d4575`
- Commit: `918834b25e3372821aa296491ca71695ea24be91`
- Unsigned: **yes**
- Proposed channel: **internal**
- Date: 2026-09-18

Local tagged package (not pushed):
`src-tauri/target/release/bundle/nsis/SaVaGe_0.1.2_x64-setup.exe`
Guest staging copy: `C:\Users\Public\SaVaGe-0.1.2-internal\`

Replaces the native close dialog with an in-app **Save / Discard / Cancel** prompt
and exits the process after confirm, so Windows CloseRequested cannot strand the
window. Ctrl+Z / Ctrl+Y are handled app-wide. Open in Editor Discard already
starts an unsaved session (from 0.1.1). Prior Guest evidence stays in
[v0.1.1.md](v0.1.1.md) and [v0.1.0.md](v0.1.0.md).

## Internal corpus

- [x] `pnpm test` passed
- [x] Rust tests passed
- [x] Compatibility fixtures recorded (copies of `fixtures/compatibility/`; originals unchanged)
- [x] Native smoke / M8.2 install notes attached or waived with reason
- Operators (names): `s_sme` (package); Guest (2026-09-18)

Guest 0.1.2: window close **Discard** quits, **Cancel** leaves the app open, Open in
Editor Discard starts an unsaved session, and Ctrl+Z works off the canvas. Stay
internal (unsigned). Remaining packaged checks: 150%/200% scaling, High Contrast,
Narrator, WebView2-missing VM.

## Beta

Not started.

## Gate review

- Decision: **stay internal**. First verified catalog row is this tag
  ([verified-installers.json](../verified-installers.json)). Authenticode is still
  required before stable.

## Halt / withdraw

No halt recorded. Withdrawing `v0.1.2` has **no prior verified installer** to
offer. Unsigned `v0.1.0` / `v0.1.1` exist locally but were not catalogued
(close did not quit). See [m8-withdraw.md](../m8-withdraw.md).
