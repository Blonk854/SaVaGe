# Promotion record — v0.1.2

- Version / git tag: 0.1.2 / `v0.1.2` (when tagged)
- Artifact SHA-256: (fill after the tagged NSIS exists)
- Commit: (fill at tag)
- Unsigned: **yes**
- Proposed channel: **internal**
- Date: 2026-09-18

Replaces the native close dialog with an in-app **Save / Discard / Cancel** prompt
and exits the process after confirm, so Windows CloseRequested cannot strand the
window. Ctrl+Z / Ctrl+Y are handled app-wide. Open in Editor Discard already
starts an unsaved session (from 0.1.1). Prior Guest evidence stays in
[v0.1.1.md](v0.1.1.md) and [v0.1.0.md](v0.1.0.md).

## Internal corpus

- [x] `pnpm test` passed
- [x] Rust tests passed
- [ ] Compatibility fixtures recorded
- [ ] Native smoke / M8.2 install notes attached or waived with reason
- Operators (names): `s_sme` (package); Guest (retest close/Open in Editor/undo)

v0.1.2 is unsigned, so the maximum channel is **beta**. It cannot be marked stable.

## Beta

Not started.

## Gate review

- Decision: **stay internal** until Guest confirms window close quits on Discard
  and Cancel leaves the app open.

## Halt / withdraw

No halt recorded. Prior unsigned installers `v0.1.0` and `v0.1.1` exist locally
but are not in the verified catalog. Do not invent a SHA. See
[m8-withdraw.md](../m8-withdraw.md).
