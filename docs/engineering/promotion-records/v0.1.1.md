# Promotion record — v0.1.1

- Version / git tag: 0.1.1 / `v0.1.1` (when tagged)
- Artifact SHA-256: (fill after the tagged NSIS exists)
- Commit: (fill at tag)
- Unsigned: **yes**
- Proposed channel: **internal**
- Date: 2026-09-18

Fixes the Guest-found close **Discard** no-op and stops Convert from replacing an
open project until **Open in Editor**. Prior unsigned `v0.1.0` Guest evidence stays
in [v0.1.0.md](v0.1.0.md).

## Internal corpus

- [ ] `pnpm test` passed
- [ ] Rust tests passed
- [ ] Compatibility fixtures recorded
- [ ] Native smoke / M8.2 install notes attached or waived with reason
- Operators (names): `s_sme` (package); Guest (retest close/convert, not yet run)

v0.1.1 is unsigned, so the maximum channel is **beta**. It cannot be marked stable.

## Beta

Not started.

## Gate review

- Decision: **stay internal** until Guest retests Save/Discard/Cancel close and Open in Editor.

## Halt / withdraw

No halt recorded. Prior unsigned installer `v0.1.0` exists locally but is not in
the verified catalog. Do not invent a SHA. See [m8-withdraw.md](../m8-withdraw.md).
