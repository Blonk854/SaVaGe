# Rollback rehearsal — v0.1.2

- Current tag: `v0.1.2`
- Prior verified installer: **none** (catalog contains only this tag)
- Date: 2026-09-18

Live prior-NSIS rehearsal is **blocked**. Do not invent a previous verified
setup. Local unsigned `v0.1.0` / `v0.1.1` NSIS files exist but were not
catalogued (window close did not quit).

Automated evidence for this version:

- Compatible originals in `fixtures/compatibility/` open from copies without write-back
- `future-v2.savage` and `future-recovery.json` stay unchanged
- `allowDowngrades` is on so a later prior installer can reinstall without wiping app data
